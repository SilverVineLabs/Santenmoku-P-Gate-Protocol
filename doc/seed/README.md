# Seed Setup

Copy `human_seed.template.md` to `human_seed.md` (add to `.gitignore`) and populate with your own natural language context.

This local file is for your personal alignment notes and should stay untracked.
